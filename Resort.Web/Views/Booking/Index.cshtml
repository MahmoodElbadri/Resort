@{
    ViewData["Title"] = "Bookings";
}
<div class="w-100 card border-0 p-4">
    <div class="card-header bg-success bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Booking List</h2>
            </div>
        </div>
    </div>
    <div class="card-body border p-4">
        @* <div class="d-flex justify-content-between pb-4 pt-2">
            <ul class="list-group list-group-horizontal-sm">
                <li class="list-group-item @(ViewBag.Status == SD.StatusPending ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusPending">
                        @SD.StatusPending
                    </a>
                </li>
                <li class="list-group-item @(ViewBag.Status == SD.StatusApproved ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusApproved">
                        @SD.StatusApproved
                    </a>
                </li>
                <li class="list-group-item @(ViewBag.Status == SD.StatusCheckedIn ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusCheckedIn">
                        @SD.StatusCheckedIn
                    </a>
                </li>
                <li class="list-group-item @(ViewBag.Status == SD.StatusCompleted ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusCompleted">
                        @SD.StatusCompleted
                    </a>
                </li>
                <li class="list-group-item @(ViewBag.Status == SD.StatusCancelled ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusCancelled">
                        @SD.StatusCancelled
                    </a>
                </li>
            </ul>
        </div> *@
        <div class="d-flex justify-content-between pb-4 pt-2">
            <ul class="list-group list-group-horizontal-sm">
                <li class="list-group-item text-white bg-primary @(ViewBag.Status == SD.StatusPending ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusPending">
                        @SD.StatusPending
                    </a>
                </li>
                <li class="list-group-item text-white bg-primary @(ViewBag.Status == SD.StatusApproved ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusApproved">
                        @SD.StatusApproved
                    </a>
                </li>
                <li class="list-group-item text-white bg-primary @(ViewBag.Status == SD.StatusCheckedIn ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusCheckedIn">
                        @SD.StatusCheckedIn
                    </a>
                </li>
                <li class="list-group-item text-white bg-primary @(ViewBag.Status == SD.StatusCompleted ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusCompleted">
                        @SD.StatusCompleted
                    </a>
                </li>
                <li class="list-group-item text-white bg-primary @(ViewBag.Status == SD.StatusCancelled ? "active" : "")">
                    <a style="text-decoration: none; color: inherit;" asp-controller="Booking" asp-action="Index" asp-route-status="@SD.StatusCancelled">
                        @SD.StatusCancelled
                    </a>
                </li>
            </ul>
        </div>

        <table id="tblBookings" class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Phone</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Check-In Date</th>
                    <th>Nights</th>
                    <th>Total Cost</th>
                </tr>
            </thead>
            <tbody></tbody>
        </table>
    </div>
</div>

@section Scripts {
    <script src="~/js/bookings.js"></script>
}